<h4> Основное место работы </h4>


<%= form_for(@main_job) do |f| %>
    
  <%= render 'shared/error_messages', object: f.object %>

  <%= render 'main_job_form', f: f %>

  <%= f.submit "Создать", class: "btn btn-large btn-success" %>

<% end %>
