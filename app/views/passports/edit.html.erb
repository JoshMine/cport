<h1>ParticipantPassports#edit</h1>
<p>Find me in app/views/participant_passports/edit.html.erb</p>

<%= form_for(@passport) do |f| %>
    
  <%= render 'shared/error_messages', object: f.object %>

  <%= render 'passport_form', f: f %>

  <%= f.submit "Сохранить", class: "btn btn-large btn-primary" %>

<% end %>
